# 開發環境的前端變數（會進版控，給所有開發者一個開箱可用的預設值）
#
# 為什麼用絕對位址，而不是 package.json 的 "proxy"：
# 只要 package.json 有 proxy，react-scripts 就會啟用 dev server 的 host 檢查
# （node_modules/react-scripts/config/webpackDevServer.config.js 的
#   disableFirewall = !proxy），而它填入 allowedHosts 的來源只接受 RFC1918
# 私有位址。機器上若有 Tailscale 之類的 CGNAT 介面（100.64.0.0/10），
# 取到的位址不算私有、會被丟成 undefined，dev server 啟動時就以
# 「allowedHosts[0] should be a non-empty string」直接崩潰。
#
# api.js 讀 process.env.REACT_APP_API_URL，有值就用它，沒有才退回同源的 /api。
# 埠號要跟 src/backend/.env 的 PORT 一致。
#
# 要改成自己的設定請開 .env.local（優先權比這個檔高，且已在 .gitignore 內），
# 不要改這個檔案。
#
# 註：正式環境用的是 .env.production（不設此變數，走同源 /api 由 nginx 轉發），
# 那裡的 PORT 是 5001，是伺服器上的埠，不要照抄到這裡。
REACT_APP_API_URL=http://localhost:5000/api
