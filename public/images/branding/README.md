# 系學會品牌素材

此目錄存放關於我們頁面（/about）使用的品牌圖片。

## 檔案

- `imsa-banner.png` — 寬版橫幅
  - 建議尺寸：2460 × 936（與原始 PNG 相同），自動 scale
  - 使用位置：`AboutUsPage.js` 頁面頂部
  - 缺檔時頁面會自動 fallback 到藍色漸層 + 名稱文字，不會破版

把檔案直接放進這個資料夾即可生效，毋需任何 build 步驟。
